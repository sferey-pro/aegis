# Database files
*.sqlite
*.db
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal

# Variables d'environnement locales.
#
# `app_build/.gitignore` les ignorait déjà, mais le fichier réellement utilisé est
# à la **racine** du dépôt : il n'était donc pas couvert, et un `git add -A` l'a
# commité avec un vrai jeton GitHub dedans (push bloqué par la protection de
# secrets, historique réécrit le 22/08/2026).
.env
.env.local
.env.*.local

# Instantanés SQLite et sauvegardes de configuration (CONTEXT.md §12)
backups/
.aegis_remote_projects/

# Agent Scratchpad
.agents/scratch/

# OS files
.DS_Store
Thumbs.db

# IDE & Editors
.idea/
.vscode/
*.swp
*.swo
*~
*.bak
*.orig

# Node / Bun & Build outputs
node_modules/
dist/
out/
build/
coverage/
*.lcov
*.tsbuildinfo
.eslintcache
.cache/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
bun.log*
